<nav class="navbar navbar-expand-md navbar-light bg-light">
    <div class="container">
      <a class="navbar-brand d-block col-1" href="/">
        <img class="img-fluid me-1" style="height: 40px;" src="/images/B.png">  <span class="text-primary h5 fw-bold">Badr</span></a>
      <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
      </button>
      <div class="collapse navbar-collapse" id="navbarSupportedContent">
        <ul class="navbar-nav col-12 offset-1 d-flex justify-content-around">

          <li class="nav-item d-inline-block">
            <a class="nav-link  <%= page === 'home' ? 'text-primary' : '' %>" aria-current="page" href="/">Home</a>
          </li>

          <% if(isAuth) { %>
          <li class="nav-item d-inline-block d-inline-block">
            <a class="nav-link <%= page === 'cart' ? 'text-primary' : '' %>" href="/cart">Cart</a>
          </li>
          <li class="nav-item d-inline-block">
            <a class="nav-link mx-1  <%= page === 'orders' ? 'text-primary' : '' %>" href="/order">Orders</a>
          </li>
          <% } %>

          <li class="nav-item d-inline-block">
            <a class="nav-link  <%= page === 'documentation' ? 'text-primary' : '' %>" aria-current="page" href="/documentation">Documentation</a>
          </li>

          <li class="nav-item d-inline-block">
            <a class="nav-link  <%= page === 'about' ? 'text-primary' : '' %>" aria-current="page" href="/about">About</a>
          </li>

          <% if(! isAuth) { %>
            <li class="nav-item d-inline-block float-end">
              <a class="nav-link" href="/signup"><button class="btn btn-sm btn-outline-primary rounded-pill px-5">Signup</button></a>
            </li>
            <li class="nav-item d-inline-block float-end">
              <a class="nav-link" href="/login"><button class="btn btn-sm btn-outline-primary rounded-pill px-5">Login</button></a>
            </li>
          <%}%>
          <% if(isAdmin) {%>
          <form action="/admin/dashboard" method="get" class="">
            <button type="submit" class="btn btn-primary rounded-pill btn-sm mt-1 px-lg-5  d-md-block">Dashboard</button>
          </form>
          <% } %>
          <% if(isAuth) {%>
          <form action="/logout" method="post" class="">
            <button type="submit" value="logout" class="btn btn-outline-primary rounded-pill btn-sm mt-1 px-lg-5 d-md-block">Logout</button>
          </form>
          <%}%>
        </ul>
      </div>
    </div>
  </nav>



  